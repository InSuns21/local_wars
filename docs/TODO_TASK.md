# TODO_TASK（MVP未完了タスク洗い出し）

現状コードを精査したうえで、**最低限1人プレイヤーが遊べる状態**に必要な未完了タスクをチェックリスト化。

## P0: 1人プレイ成立に必須

- [x] **AI手番実装（Easy/Normal）**
  - `END_TURN` 後に敵側（P2）の行動を自動実行する。
  - Easy: 近傍攻撃・占領優先の単純ロジック。
  - Normal: 占領価値＋被害回避を加えた評価。
  - 完了条件: 人間はP1のみ操作して対戦が最後まで進行する。

- [x] **AI思考結果をコマンド適用へ接続**
  - `gameStore` または `turnEngine` に `runAiTurn()` を追加。
  - AIの行動は既存 `dispatchCommand` / `applyCommand` を通す。
  - 完了条件: AIが不正コマンド（射程外攻撃など）を出さない。

- [x] **ターン開始収入（工場/司令部）実装**
  - `nextTurnState` で所有拠点数×収入を加算。
  - 設定画面の `incomePerProperty` を実際の計算に反映。
  - 完了条件: ターン経過で資金が増え、生産が継続可能。

- [x] **生産フェーズのUI導線実装**
  - 工場選択→ユニット選択→`PRODUCE_UNIT` 実行。
  - 生産不可理由（資金不足/配置不可）をUI表示。
  - 完了条件: 実プレイ中に生産がUIだけで完結する。

- [x] **勝敗確定時のゲーム終了導線実装**
  - `winner` 確定時に結果表示（勝利/敗北）とタイトル復帰導線を出す。
  - 完了条件: 対局が終了条件到達で自然に終わる。

## P1: MVP要件充足に必要

- [x] **スカーミッシュ実マップ読み込み実装**
  - 現在はマップ選択しても実体盤面は固定のため、`mapId` から実データを読んで初期化する。
  - 完了条件: 選んだマップによって盤面/初期配置が変わる。

- [x] **スカーミッシュ10マップ整備**
  - `maps.ts` メタだけでなく、実マップデータ（地形/初期配置/勝利条件）を10面用意。
  - 完了条件: 10面すべて開始可能。

- [x] **AI難易度設定の反映**
  - 設定画面の `aiDifficulty` をAIロジックへ接続。
  - 完了条件: 難易度によって行動品質が変化。

- [x] **セーブデータ項目の整合性確認**
  - `state` に加え、`mapId` / `settings` をロード時に確実復元。
  - 完了条件: つづきから再開時に試合条件が崩れない。

## P2: MVP外だが現状仕様との差分（後続）

- [x] **設定項目の実ロジック反映（索敵/燃料/弾薬）**
  - `fogOfWar`, `enableFuelSupply`, `enableAmmoSupply` はUI保存のみで戦闘ロジック未接続。

- [x] **型定義の地形/ユニット拡張整合**
  - `createInitialGameState` の航空/海トグルは将来値（AIRPORT/PORT/SEA等）を文字列で参照しているため、正式型へ統合。

## 推奨実装順（最短で遊べる順）

1. [x] ターン収入
2. [x] 生産UI
3. [x] AI手番（Easy）
4. [x] AI手番（Normal）
5. [x] 勝敗画面
6. [x] 実マップ読み込み
7. [x] 10マップ整備






## 追加要件 TODO（2026-03-02）

- [ ] **スタック拡張ルール実装（工場2/HQ3 + 生産時複数積み）**
  - 現状は全マス1ユニット制約で実装されている。
  - 要件対応には MapState 側のスタック管理構造、移動/生産/描画/AI の全面改修が必要。

- [x] **FoW遭遇戦（移動経路上の不可視敵との接触戦闘）**
  - 現状FoWは「視界外ユニット非表示」のみ。
  - 要件対応には移動経路シミュレーション中断、遭遇戦解決、移動側が先制不能の戦闘ルール追加が必要。

- [ ] **スタック例外ルール（空+地上、空+海、輸送ユニット）**
  - 将来仕様の同居・搭載ルールは未実装。
  - 上記スタック基盤と合わせて実装する。

- [x] 通常の歩兵を除く陸上ユニットは山のマスを通過できない
- [x] 歩兵の陸上ユニットは山のマスを通過できる＋視界があがる＋防御力が上がる